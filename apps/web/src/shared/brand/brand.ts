/**
 * Identidad de la plataforma.
 *
 * Responsabilidad única: ser el único lugar del código donde se escribe el
 * nombre del producto. Cambiar la marca debe ser cambiar esta constante, no
 * buscar cadenas por toda la interfaz.
 *
 * No incluye el logo: la marca es sólo texto hasta que exista uno definitivo.
 *
 * Hay una copia inevitable en `apps/web/index.html` (`<title>`): ese documento
 * se sirve antes de que arranque React, así que no puede importar de aquí. Si
 * cambia esta constante, actualiza también ese `<title>`.
 */
export const BRAND_NAME = "AUREAM OS";
